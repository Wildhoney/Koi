import test from 'ava';
import { getEntities } from '../../src/containers/world';

console.log(getEntities);

test('It should be able to render the entities;', t => {

    const props = { theme: {
        plain: 0xFF00000
    }};



});
